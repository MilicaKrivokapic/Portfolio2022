import ChevronLeft from "./ChevronLeft"
export default function ChevronRight() {
  return <ChevronLeft className="rotate-180" />
}

// Component takes left pointing chevron and rotates it 180 degrees, then returns it
